# Drafting new prose needs a key. Nothing else does: seeds, validation, the
# gate, the build, the tests and the site all work without one, and a lesson
# written by hand is treated exactly the same as a drafted one.
GEMINI_API_KEY=
