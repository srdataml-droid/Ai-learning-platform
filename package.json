{
  "name": "curriculum-zero-to-ai-engineer",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port=3000 --host=0.0.0.0",
    "build": "npm run build:data && vite build",
    "preview": "vite preview",
    "clean": "rm -rf dist",
    "test": "node --test \"tests/*.test.mjs\" && node tools/validate.mjs && node tools/verify.mjs",
    "build:data": "node tools/build-data.mjs && node tools/build-languages.mjs && node tools/build-words.mjs && node tools/build-comparisons.mjs"
  },
  "devDependencies": {
    "vite": "^6.2.3"
  }
}
